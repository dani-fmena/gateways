> This folder holds the models and model's logic for the entire solution.
> Putting this logic in a separated DLL allow us to reuse it, in another 
> services, app or APIs that share the same business domain