
/**
 * Convert binary number to its decimal representation  
 * @param bin Binary number
 * @returns {number} Decimal representation of the binary number
 */
export const toDec = (bin) => {
    return parseInt(bin,2);
} 